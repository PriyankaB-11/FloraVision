export type Plant = {
  img: string;
  name: string;
  title: string;
  price: number;
  category?: string;
};

export type Review = {
  img: string;
  name: string;
  stars: number;
  review: string;
};
